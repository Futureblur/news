---
import { Image } from 'astro:assets';
const {src, alt} = Astro.props;
---

<div class="py-8">
    <div class="-mx-6 lg:-mx-28">
        <Image src={ src } alt={ alt } class={ "w-full overflow-visible md:overflow-hidden md:rounded-lg" }
               widths={ [240, 540, 960, 1960] }
               sizes={ `(max-width: 360px) 240px, (max-width: 960px) 540px, (max-width: 1600px) 960px, ${ 1960 }px` }/>
    </div>
    <div class="font-bold text-xs text-center flex justify-between w-full">
        <div class="w-full px-2 mt-1">
            <slot/>
        </div>
        <a href={ src.src } download class="p-2 m-auto inline-flex bg-white/10 after:hidden no-underline rounded-full">
            <span class="font-black text-sm w-3 h-3 inline-flex justify-center items-center">
                ↓
            </span>
        </a>
    </div>
</div>
